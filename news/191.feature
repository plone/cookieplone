Clone failures now report a specific, actionable error: a missing VCS tool, a repository that cannot be reached, a bad branch/tag, or any other clone-time failure surface the underlying message instead of the generic `Invalid vcs repository ...`. The CLI displays the message via the standard error panel and exits cleanly, instead of letting the exception propagate as a traceback. @ericof
